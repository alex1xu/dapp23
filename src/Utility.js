/*
getDros function returns the drosophila in the matchup
returns the drosophila objects as a list
*/
export const getDros = async () => {
  return [];
};

/*
runFight function simulates the fight
requires n-dimensional list of drosophila objects
returns the winning drosophila
*/
export const runFight = (drosList) => {
  return {};
};

/*
getOdds function returns the odds of the matchup as a tuple
requires n-dimensional list of drosophila objects and mode parameter
returns the odds as a list
*/
export const getOdds = (drosList, mode) => {
  return [];
};
